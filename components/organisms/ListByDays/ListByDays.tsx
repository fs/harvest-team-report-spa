import React from 'react';
import styled from 'styled-components';

const Wrapper = styled.section`
  grid-row: 2;
  grid-column: 2;
`;

const ListByDays = () => {
  return <Wrapper>List By Days</Wrapper>;
};

export default ListByDays;
